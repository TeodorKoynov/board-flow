# Server connection settings
# Leave empty to auto-detect from Expo dev server (recommended for local development)
# Set explicitly when connecting to a remote server or specific IP
# EXPO_PUBLIC_API_HOST=192.168.1.100
EXPO_PUBLIC_API_PORT=3000
